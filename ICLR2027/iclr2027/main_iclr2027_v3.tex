% ============================================================
% ICLR 2027 — restructured on the form-vs-content spine (21-ago).
% Single source of truth: every number traces to a CSV named in a % comment.
% Figures included use ONLY clean regenerated data (fig_excess_panel,
% fig_text_nulls, fig_bestmetric_scatter) or data-independent originals
% (fig_tree_visualization, fig3_causal). The stale-data
% originals (fig_scaling, fig_synthesis_pooled, gain bubbles) are NOT
% referenced and must be regenerated before any future use.
% ============================================================
\documentclass{article}
\usepackage{iclr2027_conference,times}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{hyperref}
\usepackage{url}
\usepackage{booktabs}
\usepackage{array}
\usepackage{amsthm}
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{nicefrac}
\usepackage{microtype}
\usepackage{xcolor}
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{multirow}
\usepackage{enumitem}
\usepackage{placeins}
\usepackage{float}
\usepackage{overpic}

% Title chosen by Javi (21-ago). Alternatives kept for coauthor veto:
%  (b) The Platonic Tree Is Ours: Whose Hierarchy Foundation Models Grow
%  (c) Whose Tree Does a Foundation Model Grow? Revisiting the PRH in Geometry
% Earlier candidates ("Grow Their Own Trees", "Form, not Content") were
% partially invalidated by the tree-map result: supervised/contrastive models
% share essentially one tree.
\title{Is There a Platonic Tree? \\ Calibrating Latent Hyperbolicity \\ in Foundation Models}

\author{Anonymous authors}

%\iclrfinalcopy % Uncomment for camera-ready version, but NOT for submission.

\theoremstyle{definition}
\newtheorem{definition}{Definition}
\theoremstyle{plain}
\newtheorem{proposition}{Proposition}
\theoremstyle{remark}
\newtheorem*{remark}{Remark}
% v3: classic structure written from the claim lists (author's brief of 2026-09-18); generated by rebuttal/scripts/phaseE_v3.py. The submission file is main_iclr2027.tex.
\begin{document}

\maketitle
\raggedbottom   % final pass: [t] figures and [H] tables leave short pages; ragged bottoms instead of underfull vertical boxes

\begin{abstract}
Hyperbolic methods for representation learning rest on a premise we call latent hyperbolicity: standard models are already tree-like, because their class geometry scores a low Gromov $\delta$. We show that a low raw reading is what high dimension, an anisotropic spectrum and a maximum over sampled quadruples produce on their own, and we build an instrument that reads every score as an excess over a random cloud of the same shape, ranks it against 200 matched replicates, and adds a depth test whose false-alarm rate and power are measured on real clouds. Across 12 vision backbones, 6 datasets and 16 text models, the premise does not survive: on image features the excess sits within null noise in most cells (model-dataset pairs). Class centroids do carry clustered structure in 49 of 72 cells, but a star already produces it; hierarchy above the superclasses is certified in only 4 of 12 ImageNet backbones, by a test that never fires on randomized hubs, and a backbone trained in hyperbolic space shows the same clustering, no detected depth, and embeddings that never leave the near-flat regime. The trees are moderately shared: the naive comparison manufactures an island, and once the clustering cut is controlled every recipe recovers the human taxonomy partially, with the self-supervised tree living in angles rather than distances. Read correctly, foundation models organize classes into clustered structure that is occasionally hierarchical and moderately shared; they do not converge to one common tree, and their raw tree-likeness is not evidence for hyperbolic geometry.
\end{abstract}

\section{Introduction}
\label{sec:introduction}

Semantic knowledge is hierarchical, and hyperbolic space is the natural geometric home for hierarchies \citep{nickel2017poincare, sarkar2011low, ganea2018hyperbolic, chami2019hyperbolic}. A line of work therefore imposes it on representations at training time, from vision to vision--language and language \citep{Khrulkov_2020_CVPR, ermolov2022hyperbolic, atigh2022hyperbolic, bdeir2024fully, desai2023meru, pal2025compositional, chen2022fully, he2026helm, sinha2024learning}. These methods cite a premise, which we call \emph{latent hyperbolicity}: standard models already organize concepts in a tree-like metric structure. The evidence is a single number, the Gromov $\delta$ of their features: $\delta$ is zero for a metric tree and grows as a metric departs from one, and the measured values are low, on CNN and ViT features \citep{Khrulkov_2020_CVPR, bdeir2024fully}, on token embeddings \citep{yang2025hyperbolic} and on word vectors \citep{tifrea2019poincare}. Those readings are taken at the sample level, on individual images or tokens rather than on classes, and they are never calibrated: a value is called low on its own, without asking what a structureless cloud of the same dimension and spectrum would score. Is the premise right, and what does it license? Figure~\ref{fig:concept} shows why a low reading is not enough: a structureless cloud, a star of clusters without depth and a tree with depth cast the same shadow.

\begin{figure}[H]
\centering
\IfFileExists{figures/fig1_concept.pdf}%
{\includegraphics[width=\textwidth, trim=23 229 3 232]{figures/fig1_concept.pdf}}
{\fbox{\parbox[c][1.4in][c]{\dimexpr\textwidth-2\fboxsep-2\fboxrule\relax}{\centering \texttt{figures/fig1\_concept.pdf} (conceptual figure, drawn by the authors; same-size placeholder)}}}
\caption{\textbf{Plato's cave: a random cloud, a star and a tree cast the same shadow.} An observer who sees only shadows cannot tell which object cast them, and a raw $\delta$ is such a shadow: points with no structure, clusters around a hub, and a nested hierarchy all read as low $\delta$. The instrument reads the objects instead of the shadow: no structure beyond the null in the cloud, clusters without depth in the star, clusters with depth in the tree.}
\label{fig:concept}
\end{figure}

Why is the shadow low? Gromov $\delta$ assigns zero to trees \citep{Gromov1987}, but three artifacts push it down without any hierarchy. High-dimensional clouds concentrate toward equidistance \citep{beyer1999nearest, aggarwal2001surprising}, which is a degenerate star tree: we call this the \emph{dimension confound}, the geometric face of the width confounder of \citet{groger2026aristotelian}. Anisotropic spectra mimic low-dimensional behavior: the \emph{spectrum confound}. And the supremum over sampled quadruples is a one-quadruple extreme that does not converge \citep{fournier2015computing}: the \emph{statistic confound}. The three are visible in real models: GPT-2 M sits at its matched null despite a low raw reading, and classic sentence embedders share their raw reading with vision-contrastive models while carrying none of their structure.

We build the instrument that reads $\delta$ correctly. It calibrates the estimator on reference geometries, expresses every reading as an \emph{excess} over a null cloud matched in dimension and covariance spectrum, ranks that excess against matched replicates, and adds a \emph{depth test} whose false-alarm rate and power are measured on real clouds. With it we ask of geometry what convergence work asks of similarity \citep{huh2024platonic, groger2026aristotelian}: what survives calibration, and is it shared? They calibrate similarity across models; we calibrate geometry within a model, and comparing trees across models needs a calibration of its own.

\textbf{The answer has three parts.} On image features the excess sits within null noise in most cells and is a fraction of the null where it survives, so the premise does not survive calibration as stated. On class centroids, clustered structure is genuine in 49 of 72 cells, but a star already produces it. Hierarchy above the superclasses is certified in 4 of 12 ImageNet backbones by a test that never fires on real clouds with randomized hubs, and a backbone trained in hyperbolic space shows the same clustering and no detected depth. The trees are moderately shared: the naive comparison manufactures an island, and once the clustering cut is controlled a moderate gap remains, every recipe recovers the human taxonomy partially, more so with supervision, and the self-supervised structure lives in angles.

\textbf{Our contributions are:}
\begin{enumerate}[topsep=0pt,itemsep=1pt,leftmargin=*]
\item \textbf{The instrument.} Estimator calibration, spectrum-matched nulls read as corrected ranks, and a depth test with measured false-alarm rate and power.
\item \textbf{The census.} Clustered structure in most models, hierarchy certified in a few, and a hyperbolic backbone as the control for imposing the geometry.
\item \textbf{The map of trees.} The island is an artifact of the cut; sharing is graded and supervision-dependent, and the self-supervised tree is angular.
\item \textbf{Consequences for practice.} What to measure before imposing curvature, and which zero-cost readout collects what is there.
\end{enumerate}

\section{Related Work}
\label{sec:related}

Reading $\delta$ against a matched reference has precedent in network science, where the $\delta$ of real graphs is compared with that of random graphs of comparable size \citep{narayan2011curvature, kennedy2013hyperbolicity, adcock2013treelike}; we bring the idea to embedding clouds, where the reference must match dimension and spectrum. The analyses of learned representations characterize Euclidean structure and do not test for tree-likeness: representation similarity, intrinsic dimension and neural collapse \citep{kornblith2019cka, ansuini2019intrinsic, pope2021intrinsic, papyan2020prevalence}. Closest to us is the work on representational convergence: the Platonic Representation Hypothesis \citep{huh2024platonic} argues that models converge toward shared representations, and its cross-modal convergence has been stress-tested at scale \citep{koepke2026cave}; \citet{groger2026aristotelian} show that permutation-calibrated similarity removes most of that convergence while local neighborhood structure survives, and \citet{park2024geometry} read concept hierarchy in language models from the orthogonality of concept directions. We are the geometric complement of this program: the same calibration applied within a model instead of across models, with a depth test measured on real clouds, a hyperbolic backbone as the control for imposing the geometry, and a calibration of the tree-to-tree comparison itself.

\section{Methodology}
\label{sec:instrument}

This section fixes what is measured, how it is estimated and normalized, what it is compared with, how depth above the clusters is tested, and how trees are compared across models.

\subsection{Background: Gromov $\delta$}
\label{sec:v3-gromov}

Tree-likeness is measured on quadruples. Take any four points $w,x,y,z$ of a metric space with distance $d$. They can be paired in three ways, and each pairing has a sum of two distances:
\begin{equation}
d(w,x)+d(y,z),\qquad d(w,y)+d(x,z),\qquad d(w,z)+d(x,y).
\label{eq:pairings}
\end{equation}
Sort the three sums as $S_{\max}\ge S_{\mathrm{mid}}\ge S_{\min}$. In a tree the two largest sums are always equal, because the two pairings that cross the trunk traverse the same path, so the four-point defect $(S_{\max}-S_{\mathrm{mid}})/2$ is zero; the further a metric is from a tree, the larger the defect can become. Gromov $\delta$ is the worst case, the supremum of the defect over all quadruples \citep{Gromov1987}. A metric tree has $\delta=0$. Euclidean space has no bound at all: enlarging a square enlarges its defect in proportion. Hyperbolic space sits in between, with a bounded defect whose value for the hyperbolic plane is known, $\ln 2$ under this definition \citep{nica2016strong}.

\begin{definition}[four-point defect]
\label{def:defect}
For a quadruple $(w,x,y,z)$ with the pairing sums of Eq.~\ref{eq:pairings} sorted as $S_{\max}\ge S_{\mathrm{mid}}\ge S_{\min}$, the four-point defect is $\delta(w,x,y,z)=\tfrac12\,(S_{\max}-S_{\mathrm{mid}})$, and the Gromov $\delta$ of a metric space is the supremum of the defect over its quadruples.
\end{definition}

\subsection{Estimation and normalization}
\label{sec:v3-estimation}

Three steps turn $\delta$ into a number that can be compared across models. First, a thousand centroids have far more quadruples than can be enumerated, so the defect is computed on half a million random quadruples. Second, the largest defect in that sample is a poor estimate of the supremum: it belongs to a single extreme quadruple and keeps growing as more quadruples are drawn \citep{fournier2015computing}. We therefore read the 99.9th percentile of the sampled defects, $\hat\delta_{99.9}$, the value that 99.9 per cent of them fall below: it keeps the tail of the defects, the intent of the supremum, but it is set by hundreds of quadruples rather than by one. It also does not move with the sample size: across quadruple budgets the ImageNet reading drifts by less than half its own spread (Table~\ref{tab:q8-robust}). Third, the defect grows with the size of the point set, so we divide by the diameter, the largest pairwise distance:
\begin{equation}
\delta_{\text{norm}}=\frac{\hat\delta_{99.9}}{\mathrm{diam}}.
\label{eq:deltanorm}
\end{equation}
On this scale a tree reads zero, and a hyperbolic region of radius four or more reads below a sphere (Table~\ref{tab:q10-calibration}). What a structureless cloud reads on it is the next question.

\begin{definition}[reading of record]
\label{def:record}
The reading of record of a cloud is $\delta_{\text{norm}}$ of Eq.~\ref{eq:deltanorm}, with $\hat\delta_{99.9}$ computed over half a million sampled quadruples per seed and averaged over ten seeds.
\end{definition}
\begin{proposition}[range bound and dimension confound]
\label{prop:bound}
Let $d_{\min}$ and $d_{\max}$ be the smallest and the largest pairwise distance of a finite metric space. (a) For any quadruple the four-point defect is at most $d_{\max}-d_{\min}$, hence $\delta_{\text{norm}}\le 1-d_{\min}/d_{\max}$. (b) For an iid Gaussian cloud of $n$ points in dimension $d$, $d_{\min}/d_{\max}\to1$ as $d$ grows \citep{beyer1999nearest, aggarwal2001surprising}, so $\delta_{\text{norm}}\to0$ without any structure.
\end{proposition}
\begin{remark}
An anisotropic spectrum changes the effective dimension of the cloud and moves the same bound: the spectrum confound. A supremum over sampled quadruples is a maximum that grows with the sample: the statistic confound.
\end{remark}
The proof is in Appendix~\ref{app:proofs}. The calibration on reference geometries confirms Proposition~\ref{prop:bound} in practice: the estimator recovers the tree zero and the $\ln 2$ constant of the hyperbolic plane, spheres stay high, and iid Gaussians drift down with $d$ (Table~\ref{tab:q10-calibration}).

\subsection{Spectrum-matched null, excess and genuineness}
\label{sec:v3-null}

\begin{definition}[Haar null]
\label{def:null}
Let $X=U\Sigma V^{\top}$ be the singular value decomposition of the centered cloud. A null replicate is $X'=Q\Sigma V^{\top}$ with $Q$ Haar-distributed on the orthogonal group, so $X'$ has the singular values of $X$ and structureless coefficients.
\end{definition}
In plain words, the null is a cloud with the same shape as the real one and no structure inside it. Gaussian coefficients reproduce the spectrum only in expectation, fall short of it when $n$ is small relative to $d$, and inflate the excess of low-rank clouds. The Haar construction reproduces the sample spectrum exactly, so it is the record, and the Gaussian reading is reported alongside it in Table~\ref{tab:q1-census}.

\paragraph{Excess, rank and genuineness.}
\begin{definition}[excess]
\label{def:excess}
With $K$ null replicates $X'_1,\dots,X'_K$ of Definition~\ref{def:null}, the excess of a cell is
\begin{equation}
e(X)=\delta_{\text{norm}}(X)-\frac{1}{K}\sum_{k=1}^{K}\delta_{\text{norm}}(X'_k).
\label{eq:excess}
\end{equation}
\end{definition}
The primary evidence is the rank of the reading among its replicates and the left-tail $p$ that the rank gives,
\begin{equation}
r=\#\{k:\ \delta_{\text{norm}}(X'_k)>\delta_{\text{norm}}(X)\},\qquad p=\frac{1+K-r}{K+1},\qquad K=200.
\label{eq:rank}
\end{equation}
\begin{definition}[genuine]
\label{def:genuine}
Order the $m$ left-tail $p$-values of a census as $p_{(1)}\le\dots\le p_{(m)}$. A cell is genuine when its $p$ is at or below the Benjamini--Hochberg threshold at level five per cent,
\begin{equation}
p\ \le\ p_{(i^{*})},\qquad i^{*}=\max\{i:\ p_{(i)}\le 0.05\, i/m\},
\label{eq:bh}
\end{equation}
and the word is used in this sense only.
\end{definition}
A negative genuine excess certifies clustered structure. The word tree-like is reserved for the depth test of Section~\ref{sec:v3-depthtest}. A positive excess means less tree-like than chance given the spectrum. Figure~\ref{fig:overview} shows the two readings together: in panel (a) the null of each ImageNet backbone moves with its dimension inside a band of two null spreads, as Proposition~\ref{prop:bound}b predicts, so the gap between the filled and the hollow marker, not the level of the filled one, is the reading; in panel (b) two cells with the same raw reading receive opposite verdicts, because one null sits at the reading and the other far above it. % exp1_delta_controls.csv, expR52_census_haar_p999_200.csv, expR62_samplelevel_record.csv

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/fig_overview.pdf}
\caption{\textbf{The raw reading cannot be read on its own.} (a) Each ImageNet backbone's raw reading (filled, labeled) and what a structureless cloud of the same dimension and spectrum reads (hollow), joined by the excess; the shaded band is the null mean plus or minus two null spreads across dimension, and the dashed line the isotropic Gaussian reference. (b) Two cells with the same raw reading and opposite verdicts: on ViT-T image features the null sits at the reading, on SigLIP-B centroids far above it. % exp1_delta_controls.csv, expR52_census_haar_p999_200.csv, expR62_samplelevel_record.csv
}
\label{fig:overview}
\end{figure}

\subsection{Depth test}
\label{sec:v3-depthtest}

\begin{definition}[hub null and excess B]
\label{def:hubnull}
Given a frame that assigns each centroid $x_i$ to a cluster $k(i)$ with hub $h_{k(i)}$, the mean of its cluster, write $x_i=h_{k(i)}+o_i$. The hub null keeps every offset $o_i$ and replaces the hub configuration by a Haar resample with the hubs' spectrum, $x'_i=h'_{k(i)}+o_i$; the excess of Eq.~\ref{eq:excess} read against this null is $e_B$, and it measures hierarchy above the clusters of the frame only.
\end{definition}
\begin{definition}[matched star]
\label{def:star}
The matched star of a cloud keeps the real clusters, each as a Haar sample with the cluster's own covariance, and places their hubs as Gaussian draws at the real hub radius, so that it carries the clustering of the real cloud and no hierarchy above it. Its spectrum-matched-hub variant draws the hubs as a Haar resample of the real hubs instead.
\end{definition}
With $e_B$ read on the real cloud and on its matched star, the depth statistic and its standardized form are
\begin{equation}
\mathrm{depth}=e_B(X)-e_B(X_{\mathrm{star}}),\qquad z=\mathrm{depth}\,/\,\mathrm{s.d.},
\label{eq:depth}
\end{equation}
where the spread is taken over star seeds and null replicates, and a cloud is certified as more hierarchical than a star when $z\le-2$. The test is needed because of the star caveat: a pure star of clusters with no hierarchy already passes the census of Definition~\ref{def:genuine}, so the excess measures how classes are arranged and not whether the arrangement has depth. The frame is the WordNet cut on ImageNet and the coarse labels on CIFAR-100. The false-alarm rate and the power of the test are measured on real clouds in Section~\ref{sec:v3-depth}.

\subsection{Comparing trees across models}
\label{sec:v3-treemap}

Each model's hierarchy is a dendrogram over the shared class centroids, and two hierarchies are compared by the adjusted Rand index between their cut partitions, by the correlation between their cophenetic distances, and by their agreement on which pair of a random class triplet merges first, a measure that needs no cut. A configuration of metric and linkage is selected by a criterion fixed without reference to the agreements being compared: the highest cophenetic fidelity among configurations whose reference cuts are non-degenerate for every model, applied per dataset because degeneracy is a property of the dataset. Local agreement is read with the permutation calibration of \citet{groger2026aristotelian}: permutations of the class correspondence give the null distribution of mutual-kNN agreement and of centroid CKA, the upper quantile of that distribution gives the threshold, and the calibrated score is the excess over the threshold rescaled to keep the maximum at one.

\subsection{Projection and tasks}
\label{sec:v3-projection}

A parameter-free map centers the features, scales the 95th-percentile norm to radius $1/\sqrt2$ and preserves directions, so that Euclidean, cosine and Poincar\'{e} readouts can be compared on the intact representation. Nearest-centroid classification and 5-way 5-shot episodes score the three readouts on every cell of the task grid. The next section fixes the models, the data and the protocol on which the instrument is run. % exp1, exp2

\section{Experimental Setup}
\label{sec:v3-setup}

\paragraph{Models.} Twelve vision backbones form the census: four supervised ViTs trained on ImageNet-21k, ViT-T to ViT-L, DINO-B, four DINOv2 sizes from S to G, CLIP-B, CLIP-L and SigLIP-B. Two self-supervised ResNet-50 backbones read the same census in the appendix. Two ViT-B trained on ImageNet-1k leaf labels, DeiT-B and the augreg ViT-B, serve as the label-set control. MERU and its Euclidean CLIP twin, in three sizes, serve as the hyperbolic-backbone control. Sixteen text models read the thousand ImageNet class names: four GPT-2 sizes, three Pythia sizes, two OLMo sizes and seven sentence embedders. Parameter counts and the full panel are in Table~\ref{tab:q14-panel}.


\paragraph{Datasets and cells.} The vision census reads six class sets: ImageNet and CIFAR-100, which carry a real hierarchy, CIFAR-10 and DTD, and the flat FMNIST and MNIST. Text is read on the ImageNet class names, on DBpedia Classes, a three-level ontology independent of WordNet, and on HierarCaps, a four-level caption hierarchy. A cell is one pair of model and class set. The premise is read at the sample level, on per-image features, roughly a thousand stratified training images per cell for CIFAR-100 and DTD; hierarchy is read at the class level, on class centroids.

\paragraph{Extraction.} Vision backbones give the encoder's default pooled or CLS embedding, and class centroids average a hundred cached training images per class on ImageNet and the full cached split on the other datasets. Causal language models give the last-token hidden state of ``a photo of a \{class\}'', extracted one prompt at a time, because left-padded batching alters GPT-2's hidden states; embedders use their recommended pooling, and the CLIP and SigLIP text towers their text encoders (Appendix~\ref{app:textcensus}).

\paragraph{Replicates, seeds, budgets and frames.} Every cell is read against 200 Haar replicates, with half a million sampled quadruples per seed and ten seeds for the real cloud, and genuineness is decided by Benjamini--Hochberg over the 72 vision cells and, separately, over the text census. The depth test uses the WordNet cut into thirty superclasses on ImageNet and the twenty coarse labels on CIFAR-100, a pre-specified balanced frame as the control, a sweep over the number of clusters, and ten star seeds under the hub null. Its false-alarm rate and power are measured on synthetic hierarchies at ImageNet's class count and on the real ImageNet clouds with implanted two-level trees. With the setup fixed, we read the premise where it is read.

\section{Results}
\label{sec:form}

\subsection{The premise at the sample level}
\label{sec:v3-sample}

\paragraph{The premise does not survive calibration.} On per-image features the raw reading lies in the low band the literature reports. Calibrated against the matched null, it is not genuine in 14 of 24 cells. Where it is genuine, the excess removes at most 39 per cent of the null reading, and the largest figure belongs to DINOv2-G on CIFAR-100 images. The genuine cells belong mostly to the DINOv2 family, whose structure is the angular tree of Section~\ref{sec:content}, which cosine collects. Table~\ref{tab:q3-sample} gives the reading per cell, with the raw supremum the literature reports beside it. % expR62_samplelevel_record.csv

\subsection{Clustered structure at the class level}
\label{sec:v3-class}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/fig_excess_panel.pdf}
\caption{\textbf{Clustered structure is the rule at the class level.} Excess of the reading of record over its matched null, one panel per dataset, one marker per backbone; filled markers are genuine cells, and the shaded band around zero spans two median null spreads. Almost every marker sits left of zero, most are filled, the flat datasets scatter around zero, and the DINOv2 rows reach furthest left on CIFAR-10. % expR52_census_haar_p999_200.csv
}
\label{fig:excess}
\end{figure}

\input{tab_census}

\paragraph{Almost every cell is below its null.} On class centroids the excess is negative in 70 of 72 cells. It is genuine in 49 of 72. Table~\ref{tab:census} gives the census of record row by row, and Figure~\ref{fig:excess} shows which markers fall outside the null band. % expR52_census_haar_p999_200.csv

\paragraph{The count survives resampling.} On ImageNet and CIFAR-100, the two sets with a real hierarchy, the count is 18 of 24. Folding image resampling and estimator noise into the null, or repeating the census on thirty resampled centroid sets, leaves between 42 and 47 of the 72 cells genuine, and the cells that move are on the transfer sets (Table~\ref{tab:q8-robust}). % expR66_joint_sensitivity_summary.csv

\paragraph{The structure belongs to no single family.} Every family contributes genuine cells. The smallest backbone, ViT-T, is genuine on DTD only. The flat datasets sit near the resolution limit of ten classes, and a class-count control on ImageNet subsets shows that the reading follows the hierarchy of the labels rather than their number, as Table~\ref{tab:q8-robust} reports. Two self-supervised ResNet-50 backbones read the same clustered structure as the ViTs (Table~\ref{tab:q1-census}). % expR52_census_haar_p999_200.csv, expR60_c_sweep_record.csv, expR45_convnet_rows.csv

\paragraph{The excess is small in absolute terms and large against the null.} On ImageNet centroids the excess lies between 1 and 36 null spreads below the null mean. The absolute values are in Table~\ref{tab:census}. % expR52_census_haar_p999_200.csv

\paragraph{The star caveat.} A synthetic star of thirty clusters with no hierarchy scores an excess as deep as any real cell, reaching $-0.115$ under the Haar null. A negative excess therefore says clustered, not deep, and the depth of Section~\ref{sec:v3-depth} needs the test of Definition~\ref{def:star} (Table~\ref{tab:q10-calibration}). % expR42_star_calibration.csv

\paragraph{Neural collapse is the limit, not the reading.} Neural collapse predicts that the class means of a supervised classifier converge to a simplex equiangular tight frame \citep{papyan2020prevalence}, and an exact frame is the extreme case of the star caveat. The arrangement we certify is not that frame: class means cluster around superclass hubs that a frame would erase, cutting the centroid dendrograms recovers the CIFAR-100 superclasses, and the same clustered structure appears without class labels and in causal language models, where neural collapse is not stated (Table~\ref{tab:q11-interventions}). Scale deepens the excess for one family only: the DINOv2 rows of Figure~\ref{fig:excess} reach further left with size on CIFAR-10 and, weakly, on CIFAR-100. No other family shows a trend with scale, and training moves the raw reading within an architecture: fine-tuning on a task without class hierarchy raises it, and it falls across transformer depth, as Table~\ref{tab:q11-interventions} reports. % expR52_census_haar_p999_200.csv, analysis4_finetuning.csv, e1_delta_by_layer.csv

\subsection{Depth above the labelled clusters}
\label{sec:v3-depth}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/fig_depth_test.pdf}
\caption{\textbf{The depth test certifies hierarchy above the superclasses in 4 of 12 ImageNet backbones, never fires on randomized hubs, and misses implanted depth at ImageNet's noise level.} (a) Depth $z$ per backbone under the matched star; filled markers are certified ($z\le-2$). (b) Detection rate against implant strength on the real clouds: zero at $s=0$ and low at $s=1$ with the real within-cluster spread, full once that spread is shrunk into the validated range (dashed). % expR56_depth_variants.csv, expR64b_wn30.csv
}
\label{fig:depth}
\end{figure}

\paragraph{Depth is certified in four of twelve.} On ImageNet the real cloud is more hierarchical than its matched star in 4 of 12 backbones, ViT-S, ViT-B, ViT-L and DINOv2-L, and never less. Under the spectrum-matched-hub star the certified set is the same four, with $z$ from $-2.0$ to $-3.7$. Figure~\ref{fig:depth}a shows the four filled markers, and Table~\ref{tab:q4-depth} gives both stars per backbone. % expR56_depth_variants.csv, expR69_depth_haarhubs_summary.csv

\paragraph{The test raises no false alarm on real clouds.} With the hub arrangement of every real cloud randomized, the test fires in 0 of 60 runs. For the four certified backbones the same clusters with random hubs never fire, 0 of 20, so their verdict comes from the real hub arrangement. % expR69_depth_haarhubs_summary.csv, expR64b_wn30.csv

\paragraph{The test is weak at ImageNet's noise level.} A clean implanted two-level tree is detected in almost none of the runs, a power of 0.05. The reason is the noise level: the within-cluster spread of the real clouds is 1.3 to 3.9 times their between-hub spread, outside the range the synthetic sweep covered. Figure~\ref{fig:depth}b shows the detection rate staying at zero below full strength with the real spread, and Table~\ref{tab:q5-power} gives the power by strength. The eight other backbones are not detected, which is not the same as not hierarchical. The certified set shifts with the choice of frame. CIFAR-100, with a hundred centroids in twenty clusters, lies outside the validated regime, and its readings are reported without certification. An earlier isotropic star produced false alarms and is withdrawn. A variant with the within-cluster offsets shrunk into the validated range detects all twelve backbones, but it also fires at zero strength for two contrastive backbones, so it is a diagnostic of power and not a substitute test. % expR56_depth_variants.csv, expR64b_wn30.csv, expR64b_wn30_summary.csv, expR64b_wn30bal_summary.csv

\paragraph{Leaf-label supervision can produce the depth.} The augreg ViT-B trained on ImageNet-1k leaf labels is certified, at $z=-3.03$ under the Gaussian-hub star and under the spectrum-matched one alike. It is also aligned with WordNet, at a Spearman correlation of $+0.52$, like the ImageNet-21k ViTs. % expR70_inet1k_supervised.csv

\paragraph{But it does not guarantee it.} DeiT-B, trained on the same leaf labels with another recipe, is not certified and is nearly unaligned, at $+0.08$. It nevertheless recovers the CIFAR-100 superclasses from its dendrograms at an adjusted Rand index of 0.56. Hierarchical label sets are therefore not the explanation of the certified ViTs' depth, and alignment with the human taxonomy is recipe-dependent even among leaf-supervised ViTs (Table~\ref{tab:q4-depth}). % expR70_inet1k_supervised.csv

\paragraph{Imposing the geometry does not create detected depth.} MERU embeds images and text on the Lorentz hyperboloid with an entailment objective, and its released Euclidean twin shares backbone, data and recipe minus the hyperbolic lift \citep{desai2023meru}. The two read the same excess on ImageNet images, MERU's reading in its own Lorentz metric equals its Euclidean reading to the fourth decimal, and neither is detected as more hierarchical than a matched star, with MERU never ahead of its twin. Its embeddings never leave the near-flat regime of the hyperboloid: the spatial norm times the square root of the curvature stays between 0.258 and 0.279 at the median. Lorentz and Euclidean pairwise distances agree at a ratio of $0.997$, so the control tests the training objective rather than a curved geometry (Table~\ref{tab:q4-depth}). % expR63_meru_record.csv, expR71_meru_radii.csv

\subsection{Whose tree}
\label{sec:content}

\begin{figure}[t]
\centering
\includegraphics[width=0.6\linewidth]{figures/fig_treemap_controls.pdf}
\caption{\textbf{The categorical island is a chaining artifact; about a third of the within-block agreement is missing under most configurations.} Pairwise ARI between the dendrogram cuts of the 12 vision models on ImageNet under the naive configuration (a) and the selected one (b), with the mean DINOv2-vs-block ARI above each panel. % exp22, exp23
}
\label{fig:treemap}
\end{figure}

\paragraph{The naive map manufactures an island.} Under Euclidean distances with average linkage the DINOv2 family agrees with the supervised and contrastive block at an adjusted Rand index of 0.03 against 0.64 within the block. The island is a chaining artifact: DINOv2's Euclidean dendrograms put almost every class in one cluster, so the index is forced to zero by construction, and DINO-B does not fall in the island. Figure~\ref{fig:treemap}a shows the naive map. % exp23_treemap_controls.npz, exp23_config_diagnostics.json

\paragraph{Controlling the cut leaves a moderate gap.} Under the criterion-selected configuration, cosine distances with average linkage, the DINOv2 family agrees with the block at 0.38 against 0.48 within the block (Figure~\ref{fig:treemap}b). % exp23_treemap_controls.npz

\paragraph{The gap holds under every admissible cut.} Under the other three admissible configurations the DINOv2 family agrees with the block at 0.13 to 0.17. Within the block the agreement is 0.50 to 0.55, so about a third of it is missing under most configurations. % exp23_treemap_controls.npz, expR58_treemap_cutfree_summary.csv

\paragraph{The gap depends on the measure.} In cophenetic correlation the gap stays at 0.48 against 0.78. In triplet agreement, which needs no cut, it closes to 0.77 against 0.76 under cosine-average and persists under every other configuration (Table~\ref{tab:q6-treemap}). % expR58_treemap_cutfree_summary.csv

\paragraph{The self-supervised tree is angular.} DINOv2's semantics concentrate in the angular component of its representation. For DINOv2-L, sibling triplets on CIFAR-100 are resolved at 0.91 against 0.71 under cosine and under Euclidean distance respectively. For DINOv2-G the two figures are 0.85 and 0.63. This angular tree is why the Euclidean dendrograms chain into the island and why the triplet gap closes only under cosine-average. % exp10_local_vs_global.csv

\paragraph{The cosine census agrees with the Euclidean one.} Repeating the census on L2-normalized centroids with geodesic distances and the same Haar null, the verdict matches the Euclidean record in 65 of 72 cells, and DINOv2 on ImageNet is genuine under both readings. The cosine census is robustness, never the record (Table~\ref{tab:q1-census}). % expR57_census_cosine_haar_p999_200.csv

\paragraph{WordNet alignment follows supervision and recipe.} The Spearman correlation between inter-centroid and WordNet distances on ImageNet is $+0.57$ to $+0.59$ for the contrastive VLMs. It is $+0.49$ to $+0.53$ for the supervised ViTs. % exp3_alignment.csv

\paragraph{DINOv2 is the least aligned and the most robust.} For DINOv2 the correlation is $+0.18$ to $+0.22$, with shuffle and Gaussian controls at zero. Supervision deepens the resemblance to the human taxonomy on average across the supervised backbones tested, and the DeiT-B case of Section~\ref{sec:v3-depth} shows that the effect is recipe-dependent. Across the three families, form and content anti-correlate: the most aligned families carry the most fragile excess, and DINOv2, the least aligned, the most robust one (Table~\ref{tab:census}). % exp3_alignment.csv, expR52_census_haar_p999_200.csv

\paragraph{The recovery is not WordNet circularity.} On DBpedia Classes, an ontology independent of WordNet, the three text embedders read a genuine excess of $-0.017$ to $-0.021$ with every replicate above the real value. Their sibling-triplet agreement there is 0.89, and no clustering configuration degenerates. % expR61_dbpedia_record.csv, exp14_dbpedia.csv, exp27_dbpedia_treemap.json

\paragraph{The same reading holds on a four-level caption hierarchy.} On HierarCaps the text encoders resolve sibling triplets at 0.83 to 0.97 and order the levels by projection radius, with no class set involved (Table~\ref{tab:q7-wordnet}). % exp16_hierarcaps.csv

\paragraph{The text census depends on recipe, scale and probe.} On the thousand ImageNet class names, 7 of 15 text models are genuine under the record. GPT-2 S is genuine at $p=.020$ and flips under the supremum and under cosine, GPT-2 M is not genuine, and GPT-2 L and XL are genuine under every reading. Pythia is genuine at every scale tested, and so is OLMo-1B. The sentence embedders are absent on the class names and present on DBpedia, so the probe decides their verdict. Prompt template and extraction protocol move the fragile values but not the scale ordering (Table~\ref{tab:q2-text}). % expR53_text_haar_p999_200.csv, expR49_template_nulls_bs1.csv, expR47_extraction_variance.csv

\subsection{What is shared is local and ordinal}
\label{sec:v3-local}

\paragraph{Local agreement survives calibration.} Permutation-calibrated mutual-kNN agreement over the sixty-six model pairs is 0.464, and every pair is significant at the resolution of the permutation null. Calibrated centroid CKA is 0.577, and the Poincar\'{e} readout improves neither reading. With the cut-free measures of Section~\ref{sec:content}, this is the reading of \citet{groger2026aristotelian} on geometry: shared neighborhoods, a model-specific metric, and what a practitioner can collect from them is the subject of the next section (Table~\ref{tab:q13-local}). % exp21b_local_global_K200.csv

\section{Implications for Hyperbolic Representation Learning}
\label{sec:practice}

\paragraph{The raw reading cannot select a curvature.} \citet{Khrulkov_2020_CVPR} set the curvature of the Poincar\'{e} ball from the measured relative hyperbolicity,
\begin{equation}
c=\big(0.144/\delta_{\text{rel}}\big)^{2},\qquad \delta_{\text{rel}}=2\delta/\mathrm{diam}.
\label{eq:curvature}
\end{equation}
Evaluated on the Gaussian band of Table~\ref{tab:q10-calibration}, Eq.~\ref{eq:curvature} assigns structureless clouds a curvature from $0.48$ at $d{=}192$ to $2.5$ at $d{=}1536$, so the rule tracks the backbone's width rather than its hierarchy. % exp1_delta_controls.csv

\paragraph{The calibrated reading predicts the zero-cost gain.} Within each hierarchical dataset, the record excess predicts the nearest-centroid gain of the Poincar\'{e} readout on all four datasets and the few-shot gain on three, with confidence intervals that exclude zero and a family control that leaves the within-dataset relation in place. The depth verdict predicts no gain anywhere (Table~\ref{tab:q9-corollary}). % expR68_corollary_excess.csv

\paragraph{A zero-cost readout collects what is there.} Cosine collects the self-supervised structure. The Poincar\'{e} readout adds from $+0.9$ to $+1.3$ pp over cosine for the contrastive VLMs on prototype tasks, and its effect on the other families is inconsistent in sign. Nothing helps on flat labels, and sample-level tasks never benefit. Before imposing curvature, then, a practitioner should read the excess and its rank, the depth test, whose positive verdict certifies and whose negative verdict says nothing, and the cosine reading that says where the structure lives (Table~\ref{tab:q9-corollary}). % exp2b_normalized_stack.csv, exp2_metric_controls.csv, exp24_val_metric_selection.csv

\section{Conclusion and Limitations}
\label{sec:discussion}

\paragraph{Conclusion.} Read correctly, foundation models organize classes into clustered structure that is occasionally hierarchical and moderately shared; they do not converge to one common tree, and their raw tree-likeness is not evidence for hyperbolic geometry. The premise of latent hyperbolicity is confounded, and its sample-level reading sits within null noise. What survives at the class level is clustered structure in most cells, hierarchy above the superclasses in a few backbones, no detected depth from imposing the geometry, and a moderately shared, supervision-dependent recovery of the human taxonomy, angular for the self-supervised family. Curvature should be measured rather than assumed, and a zero-cost readout collects what is there.

\paragraph{Limitations.} (i)~The excess certifies structure beyond second moments, star-like clustering included, and depth needs the separate test. (ii)~The depth test is conservative and weak at ImageNet's noise level, certifies hierarchy above its frame only, leaves hierarchy within the clusters untested, and has no trained positive control that settles it, as Table~\ref{tab:q5-power} shows. (iii)~The census is class-centroid geometry on six datasets. (iv)~Causal-LM values, not their scale ordering, are template-conditioned. (v)~The objective--geometry link is correlational, resting chiefly on DINOv2's scale range and on two leaf-label ViTs. (vi)~Downstream scope is prototype-based metric tasks with modest gains. (vii)~The descriptor $\xi$ tracks norm dispersion rather than curvature (Table~\ref{tab:q12-xi}), and a causal LM's class representation depends on the extraction protocol as much as on the model. (viii)~The sample-level reading covers two datasets and one image budget.

\subsubsection*{Reproducibility Statement}
Every number in the paper is annotated in the source with the result file that produces it, all tables are generated by script from those files (Table~\ref{tab:provenance}), and the full pipeline with fixed seeds will be released in the anonymized repository (\texttt{TODO(author)}). Its \texttt{tool/} directory ships the instrument as one script that reproduces any cell of Table~\ref{tab:census} from its centroid matrix, and its output on two cells is checked against the table at every freeze. Estimator settings are stated in Section~\ref{sec:v3-setup} and the model panel in Table~\ref{tab:q14-panel}.

\subsubsection*{Ethics Statement}
This work analyzes publicly released models and standard public datasets, involves no human subjects and no personal data, and proposes an analysis methodology and a metric-selection heuristic. We do not foresee ethical risks beyond those already associated with the underlying models and datasets.

\subsubsection*{AI Use Statement}
In this work, we used generative AI tools for four tasks: writing assistance; drafting text from the authors' outlines and results; implementation and execution of the experiments under the authors' direction, with simulated reviews used as methodological feedback; and retrieval of references. We have not used generative AI tools for research ideation, and the remaining tasks with required disclosure are not applicable to this work. We have reviewed all AI-assisted work: the authors verified all code, numbers and references, every number in the paper is regenerated by script from the released result files and checked by an automated sweep at every freeze, and all experimental designs, results and claims were checked and approved by the authors. We take responsibility for the final content of this work, including text, claims or artifacts produced with the aid of generative AI.

\FloatBarrier
\bibliographystyle{iclr2027_conference}
\bibliography{references}

\clearpage
\appendix
\raggedbottom
\section{Proofs}
\label{app:proofs}

\begin{proof}[Proof of Proposition~\ref{prop:bound}]
(a) Each pairing sum in Eq.~\ref{eq:pairings} adds two distances of the space, each of which lies in $[d_{\min}, d_{\max}]$. Hence $S_{\max}\le 2d_{\max}$ and $S_{\mathrm{mid}}\ge 2d_{\min}$, so the defect $\tfrac12(S_{\max}-S_{\mathrm{mid}})$ is at most $d_{\max}-d_{\min}$ for every quadruple, and so is its supremum $\delta$. Dividing by the diameter $d_{\max}$ gives $\delta_{\text{norm}}\le 1-d_{\min}/d_{\max}$; the same bound holds for the 99.9th percentile of the sampled defects, which never exceeds their supremum.
(b) Let $x_1,\dots,x_n$ be iid standard Gaussian in $\mathbb{R}^d$ with $n$ fixed. For any pair $i\ne j$ the coordinates of $x_i-x_j$ are iid with variance $2$, so $\|x_i-x_j\|^2/(2d)\to1$ almost surely as $d\to\infty$ by the law of large numbers, and the convergence holds jointly over the finitely many pairs. Therefore $d_{\min}/d_{\max}\to1$, and by part (a) $\delta_{\text{norm}}\to0$. The rate is the distance-concentration rate of \citet{beyer1999nearest} and \citet{aggarwal2001surprising}: the relative contrast $(d_{\max}-d_{\min})/d_{\min}$ vanishes as $d^{-1/2}$. For a cloud with covariance $\Sigma$ the same argument runs with $d$ replaced by the effective dimension $(\operatorname{tr}\Sigma)^2/\operatorname{tr}(\Sigma^2)$, which is the spectrum confound of the remark, and a maximum over a growing sample of quadruples is non-decreasing in the sample size, which is the statistic confound.
\end{proof}

\section{One table per question}
\label{app:tables}
Every table is generated by script from the released per-cell result files, and no value is hand-transcribed; the tables are numbered in the order the main text first cites them, one per question, with their result files indexed in Table~\ref{tab:provenance}.

\FloatBarrier
\subsection{Is the reading robust? Quadruple budget, null construction, resampling and class count}
\label{app:robust}
\label{app:csweep}
\paragraph{Hierarchy depth, not class count.} Because $C$ sets the quadruple population, we sweep ImageNet subsets $C\in\{10,\dots,1000\}$ in two compositions: random (spanning the full hierarchy) and WordNet-coherent (siblings, effectively \emph{flat}). At fixed $C$, random subsets are more exploitable than coherent ones and carry a larger excess at small $C$ (CLIP-L at every $C$, DINOv2-L up to $C{=}100$, DINOv2-G up to $C{=}20$; beyond that the two compositions converge). Random subsets still score $-0.08$ to $-0.14$ at $C{=}10$, so MNIST's flat verdict is not a class-count artifact, and DINOv2-G stays below its null at every $C$ (excess $-0.008$ at $C{=}1000$, $r{=}200$), where the supremum had placed it at the null. % expR60_c_sweep_record.csv, exp19_c_sweep.csv
\input{appendix_tables/tab_q08_robust}

\FloatBarrier
\subsection{Is a low raw reading evidence? Calibration on reference geometries and synthetic clusters}
\label{app:calibration}
\label{app:star}
\input{appendix_tables/tab_q10_calibration}

\FloatBarrier
\subsection{Is the clustered structure genuine? The vision census under every construction}
\label{app:census}
\label{app:nulls}
\paragraph{What each null preserves.} The spectrum-matched null, Gaussian or Haar, preserves $n$, $d$, the mean and the covariance spectrum and destroys all higher-order structure; the PC-permutation null preserves each principal component's marginal and destroys the dependence across components; the label shuffle keeps the point cloud itself and destroys the embedding--label correspondence; and the hub-randomizing null keeps each cluster's geometry and hub assignment and destroys the hub arrangement, which is why it serves the depth test only when read against a matched star. ``Genuine'' in the text always means beyond the first of these. In plain terms, the spectrum null is a cloud with the same shape as the real one and no structure inside it; the hub null of Section~\ref{sec:form} keeps every cluster of the real cloud and only rearranges where the clusters sit.
\input{appendix_tables/tab_q01_census}

\FloatBarrier
\subsection{The model panel and extraction}
\label{app:panel}
\input{appendix_tables/tab_q14_panel}

\FloatBarrier
\subsection{Does the premise survive at the sample level? The census on per-image features}
\label{app:sample}
\input{appendix_tables/tab_q03_sample}

\FloatBarrier
\subsection{Is the structure learned? Training interventions and curvature by edge type}
\label{app:interventions}
\label{app:orc}
\paragraph{Clustered structure tracks training.} Two interventions move it (Figure~\ref{fig:causal}; raw $\delta$, within architecture): fine-tuning on a task with no class hierarchy raises $\delta$ by 19--91\%, and $\delta$ falls across transformer depth. Shuffling the embedding-to-label correspondence leaves it unchanged, as a null-invariant quantity should. Random-weight networks give higher raw $\delta$ but near-Gaussian features, so they are not counted as evidence; excess-based versions of these interventions await stored point clouds. On matched architecture (ViT-B supervised, DINO and CLIP) all three objectives yield a genuine excess on most datasets, and the family separations emerge with scale rather than at fixed size. High mean Ollivier--Ricci curvature coexists with tree-likeness because they live on different edges: negative curvature concentrates on between-superclass bridges, about twice as often as within superclasses on ImageNet, while the mean measures within-cluster compactness (Table~\ref{tab:q11-interventions}). % analysis4_finetuning.csv, e1_delta_by_layer.csv, e5_random_control.csv, night/arch_matched_triplet.csv, night/orc_bridges.csv
\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth]{figures/fig3_causal.pdf}
  \caption{\textbf{The raw reading is learned: training interventions move it.} Plotted: raw $\delta$ within architecture, (a)~after non-hierarchical fine-tuning against the pretrained value, and (b)~across transformer depth (layers $\ge1$). % analysis4_finetuning.csv, e1_delta_by_layer.csv
}
  \label{fig:causal}
\end{figure}
\input{appendix_tables/tab_q11_interventions}

\FloatBarrier
\subsection{Is the structure deeper than a star? The depth test}
\label{app:depth}
\begin{figure}[H]
\centering
\includegraphics[width=0.55\linewidth]{figures/fig_depth_cifar100.pdf}
\caption{\textbf{The depth test on CIFAR-100 ($K{=}20$, five points per cluster) reads in the unvalidated regime.} Plotted: excess B under the hub-randomizing null for the real centroids (filled) and their matched stars (hollow circle: isotropic, withdrawn; square: anisotropic), with the $z$ of real minus anisotropic star under each backbone. % expR56_depth_variants.csv, expR55b_depth_power_leafframe.csv
}
\label{fig:depth-c100}
\end{figure}
\paragraph{Real-data readings by regime.} ImageNet ($n{=}1000$, WordNet $K{=}30$) lies in the validated regime: the real cloud is more hierarchical than its anisotropic matched star beyond the noise ($z\le-2$) in 4 of 12 backbones (ViT-S, ViT-B, ViT-L, DINOv2-L), never less, under Gaussian and under Haar-resampled star hubs alike (Table~\ref{tab:q4-depth}, Figure~\ref{fig:depth}a). CIFAR-100 ($n{=}100$, $K{=}20$) lies in the unvalidated regime, where pure stars are declared more hierarchical in 17\% of runs; its readings (3 of 12 with $z\le-2$, none with $z\ge+2$) are reported without certification (Figure~\ref{fig:depth-c100}). Validity domain of the test as run: $n\approx1000$; at $n{=}100$ the false-alarm rate is 0\% with $K{=}6$ (17 points per cluster) and 17\% with $K\ge12$, so the test is not validated below about ten points per cluster. The bar was fixed before the sweep; the restriction to the validated regime was adopted after it. % expR56_depth_variants.csv, expR55b_depth_power_leafframe.csv
\paragraph{A trained control, inconclusive.} We fine-tuned the census ViT-B/16 for two passes over the images behind its ImageNet centroids with two objectives on the same batches: cross-entropy alone, and cross-entropy plus a hierarchical cross-entropy over the WordNet superclasses. Both fine-tuned models keep the census excess of the frozen backbone ($-0.015$ and $-0.016$ against $-0.016$) and both are certified by the depth test, with $z$ of $-2.33$ and $-2.77$ against $-3.62$ for the frozen backbone: the hierarchical term lowers $z$ relative to plain cross-entropy but neither reaches the frozen reading, so the control neither confirms nor refutes the test (Table~\ref{tab:q4-depth}). The full training set could not be read at training speed from the archive, so the schedule is two passes over the census subset. % expR65_hier_finetune.csv
\input{appendix_tables/tab_q04_depth}

\FloatBarrier
\subsection{How much depth can the test see? Power on synthetic and implanted hierarchies}
\label{app:power}
\begin{figure}[H]
\centering
\includegraphics[width=0.5\linewidth]{figures/fig_depth_power_app.pdf}
\caption{\textbf{The depth test has power on synthetic hierarchies at ImageNet's class count when the within-cluster noise is below the between-hub spread.} Plotted: power ($z\le-2$) on synthetic two- and three-level hierarchies with the frame at the leaf clusters (solid, one line per $K$) or at the top level (dotted), against the within/between noise ratio, with the false-alarm rates of pure stars dashed. % expR55b_depth_power_leafframe.csv, expR55_depth_power.csv
}
\label{fig:depthpower}
\end{figure}
\input{appendix_tables/tab_q05_power}

\FloatBarrier
\subsection{Is the island real? The tree map under six configurations}
\label{app:treemap}
Threshold sensitivity of the selection criterion of \S\ref{sec:content}: the selected configuration is stable for degeneracy bounds in $[0.45,0.50]$ on ImageNet and $[0.40,0.50]$ on CIFAR-100; tighter bounds select cosine-complete on ImageNet (gap 0.13) and cosine-Ward on CIFAR-100 (gap 0.56), and in every case the label-free gap is moderate and configuration-sensitive, never a categorical island. The criterion must be applied per dataset: the configuration it selects on DBpedia (Euclidean, average linkage, where nothing is degenerate) is exactly the degenerate naive configuration on ImageNet, so a criterion transferred across datasets would reintroduce the island. % exp23_config_diagnostics.json, exp23_treemap_controls.npz, exp27_dbpedia_treemap.json
\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{figures/fig_treemap_c100.pdf}
\caption{\textbf{The island is an artifact on CIFAR-100 too.} Plotted: the same reading as Figure~\ref{fig:treemap} on the 20 CIFAR-100 superclass cuts, naive (a) against criterion-selected (b). % exp23_treemap_controls.npz
}
\label{fig:treemapc100}
\end{figure}
\input{appendix_tables/tab_q06_treemap}

\FloatBarrier
\subsection{Whose taxonomy? WordNet alignment, superclass recovery, DBpedia and HierarCaps}
\label{app:wordnet}
\label{app:hierarcaps}
On HierarCaps \citep{alper2024hierarcaps} (1000 four-level caption chains), text encoders show the same reading with no class set: the projection radius orders levels within a chain, sibling triplets are resolved at 0.83--0.97 (chance $0.5$), and the leaf excess repeats the text census (Table~\ref{tab:q7-wordnet}). % exp16_hierarcaps.csv
\input{appendix_tables/tab_q07_wordnet}

\FloatBarrier
\subsection{Is text the same? The text census under the record}
\label{app:textcensus}
\label{app:templates}
\paragraph{Extraction, anisotropy and templates.} Extraction is part of the measurement: left-padded batching moves GPT-2's $\hat\delta$ by up to 0.018, of the order of the fragile excesses; the padding-free protocol is the record. Genuineness is not a proxy for anisotropy: GTE-base has the highest effective rank and is not genuine, and the only LLM-backboned embedder tested (GTE-Qwen2-1.5B) is not genuine under the record. Causal-LM values are template-sensitive but the scale ordering is template-robust (Table~\ref{tab:q2-text}). % expR53_text_haar_p999_200.csv, exp18b_text_anisotropy.csv, expR49_template_nulls_bs1.csv, expR47_extraction_variance.csv
\begin{figure}[H]
\centering
\includegraphics[width=0.8\linewidth]{figures/fig_text_nulls.pdf}
\caption{\textbf{In text, clustered structure depends on recipe and scale: robust at the larger GPT-2 sizes, fragile at the smaller.} Plotted: the text census of record (padding-free extraction, Haar null, 99.9th-percentile statistic, 200 replicates), with error bars combining null and estimator s.d., hollow bars for cells that are not genuine, and OLMo-7B omitted as not re-extracted. % expR53_text_haar_p999_200.csv
}
\label{fig:textnulls}
\end{figure}
\input{appendix_tables/tab_q02_text}

\FloatBarrier
\subsection{Is local structure shared? Permutation-calibrated agreement}
\label{app:local}
\input{appendix_tables/tab_q13_local}

\FloatBarrier
\subsection{What follows for practice? The corollary}
\label{app:corollary}
\label{app:familycorr}
\paragraph{One parameter-free projection and two tasks serve the corollary.} The parameter-free map $\phi(x)=\tanh(\tfrac12\|s(x-\mu)\|)\cdot\frac{s(x-\mu)}{\|s(x-\mu)\|}$ centers features, scales the 95th-percentile norm to radius $1/\sqrt2$, and preserves directions; prototypes in the ball are means of projected points re-mapped through the origin. Tasks: nearest-centroid (NC) and 5-way 5-shot (FS, 1000 paired episodes); metric arms: Euclidean (R), Poincar\'{e} (H), the radial map with Euclidean distances (RT), cosine (COS), and the L2-normalized stack. Changing the readout metric on the intact configuration, which is what the rule selects among, is distinct from deforming the configuration itself. % exp1, exp2

\paragraph{The calibrated reading predicts the gain within datasets.} Within each hierarchical dataset the Poincar\'{e} advantage tracks the record excess: the nearest-centroid correlation is $-0.73$ on ImageNet with a confidence interval that excludes zero on all four datasets, the few-shot correlation excludes zero on CIFAR-100, CIFAR-10 and DTD but not on ImageNet, and the depth-test $z$ predicts no gain anywhere (Table~\ref{tab:q9-corollary}). The raw reading correlates as strongly ($-0.84$ on ImageNet for nearest-centroid) and survives partialing out dimension, baseline accuracy, family and spectral covariates; without DINOv2 it holds on ImageNet and CIFAR-100 but not on CIFAR-10/DTD. Pooled cross-dataset correlations are family-driven and not used as evidence; on flat-label datasets no metric helps. Gains carry paired confidence intervals and a McNemar test. % expR68_corollary_excess.csv, table1_regenerated.csv, night/correlation_cis.csv, exp13_mcnemar.csv

\paragraph{The training objective picks the metric.} The radial map with Euclidean distances changes nothing, so the gain is the metric itself, and the split follows the objective: cosine suffices for DINOv2, Poincar\'{e} $\approx$ cosine for supervised ViTs, and Poincar\'{e} beats cosine for CLIP by up to +1.3pp even after L2 normalization (Figure~\ref{fig:bestmetric}; held-out selection and policy comparison in Table~\ref{tab:q9-corollary}). The rule is read off the families it is scored on: it describes them rather than validating a policy. % exp2, exp2b, exp24

\paragraph{A diagnostic, not a policy.} \citet{Khrulkov_2020_CVPR} estimate $c\approx0.33$ from the raw reading of their datasets; the same rule on structureless Gaussian clouds gives the curvatures of Section~\ref{sec:practice}. Cosine is a safe default, gating on the reading is the weakest policy tested, and gains are modest and limited to prototype-based tasks (sample-level tasks never benefit, Table~\ref{tab:q3-sample}). On DBpedia, a three-level text hierarchy, the diagnostic predicts correctly on both sides: genuine excess, no metric gain. % exp2, exp20, exp24, exp14, exp15
\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{figures/fig_bestmetric_scatter.pdf}
\caption{\textbf{Cells with a deeper calibrated excess carry larger zero-cost metric gains.} Plotted: best zero-cost metric minus Euclidean per cell against the record excess (FS left, NC right), one marker per dataset and family colors; the within-dataset correlations with confidence intervals are in Table~\ref{tab:q9-corollary}. % expR52_census_haar_p999_200.csv, exp2_metric_controls.csv, expR68_corollary_excess.csv
}
\label{fig:bestmetric}
\end{figure}
\input{appendix_tables/tab_q09_corollary}

\FloatBarrier
\subsection{Does $\xi$ read curvature? The parallelogram defect as a norm-structure descriptor}
\label{app:xi}
\paragraph{$\xi$ reads norm structure, not curvature.} A parallelogram estimator $\xi$, intended as a curvature sign, is driven negative by heterogeneous norms alone, so it is reported only as a norm-structure descriptor. % exp12, expR43_xi_norm_references.csv
For a triangle $(a,b,c)$ with $m$ the approximate midpoint of $(b,c)$ (the minimizer of $D(\cdot,b){+}D(\cdot,c){+}|D(\cdot,b){-}D(\cdot,c)|$), the parallelogram defect is $\xi = \big(D(a,m)^2 + \tfrac14 D(b,c)^2 - \tfrac12 D(a,b)^2 - \tfrac12 D(a,c)^2\big)\,/\,(2\,D(a,m)\,\mathrm{diam})$, averaged over $2{\times}10^4$ sampled triangles: $0$ in Euclidean space, positive on spheres, negative in hyperbolic space and on trees.
\input{appendix_tables/tab_q12_xi}

\FloatBarrier
\subsection{Provenance}
\label{app:provenance}
\input{appendix_tables/tab_z_provenance}



\end{document}
