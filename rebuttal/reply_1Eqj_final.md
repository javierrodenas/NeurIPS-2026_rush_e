Thank you for this final comment and for the reference, which is exactly the right one.

On Q2: the reviewer is correct, and we accept the point in full. $\ln(1+\sqrt2)\approx0.881$ is the thin-triangles constant of $\mathbb{H}^2$, a different definition from the four-point condition of Eq. 1; for the four-point definition the correct reference value is $\ln 2\approx0.693$ (Nica and Spakula, 2016, as the reviewer indicates). We note that our new calibration already measures exactly this: the $\mathbb{H}^2$ rows converge to 0.69 as the region grows, so the estimator empirically recovers the correct constant for the definition it implements. The revision will state both constants with their definitions, cite the reference above, and remove the mismatched attribution; we thank the reviewer for supplying the precise citation.

On Q3: agreed, and this is how the revision is already specified: the excess over the matched null becomes the primary reported quantity, and the capacity trend is re-established on that basis in the current record (e.g. DINOv2 S→G on CIFAR-10: excess −0.087 → −0.146, deepening with scale). "Low $\delta$" will be defined as "below the matched null", never as an absolute property.

We understand the score decision and thank the reviewer for an exchange that has made the metric foundations of the paper substantially more precise.
