% ============================================================
% ICLR 2027 — restructured on the form-vs-content spine (21-ago).
% Single source of truth: every number traces to a CSV named in a % comment.
% Figures included use ONLY clean regenerated data (fig_excess_panel,
% fig_text_nulls, fig_bestmetric_scatter) or data-independent originals
% (fig_intro_concept, fig_tree_visualization, fig3_causal). The stale-data
% originals (fig_scaling, fig_synthesis_pooled, gain bubbles) are NwOT
% referenced and must be regenerated before any future use.
% ============================================================
\documentclass{article}
\usepackage{iclr2027_conference,times}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{hyperref}
\usepackage{url}
\usepackage{booktabs}
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{nicefrac}
\usepackage{microtype}
\usepackage{xcolor}
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{multirow}
\usepackage{enumitem}
\usepackage{placeins}
\usepackage{float}

% Title chosen by Javi (21-ago). Alternatives kept for coauthor veto:
%  (b) The Platonic Tree Is Ours: Whose Hierarchy Foundation Models Grow
%  (c) Whose Tree Does a Foundation Model Grow? Revisiting the PRH in Geometry
% Earlier candidates ("Grow Their Own Trees", "Form, not Content") were
% partially invalidated by the tree-map result: supervised/contrastive models
% share essentially one tree.
\title{Is There a Platonic Tree? \\ Calibrated Class Geometry in Foundation Models}

\author{Anonymous authors}

%\iclrfinalcopy % Uncomment for camera-ready version, but NOT for submission.

\begin{document}

\maketitle

\begin{abstract}
Foundation models are often claimed to converge toward shared representations; recent calibrated analyses show that much of that convergence reflects uncalibrated similarity measures. We ask the same question of geometry: what structure of a model's inter-class representation survives calibrated measurement, and is it shared across models? We measure tree-likeness (Gromov $\delta$-hyperbolicity, Ollivier--Ricci curvature) for class representations of frozen foundation models, calibrating every reading on reference geometries and against nulls matched in dimension and covariance spectrum, since raw $\delta$ is confounded by dimension, class count, and spectrum. Three findings emerge. (1)~Genuine tree-like \emph{form} is widespread but not universal: 69 of 72 vision model$\times$dataset combinations sit below their matched null (on the four hierarchical-label datasets, 41 of 48 cells beyond two null standard deviations), while in text it depends on the training recipe and scale, emerging with size in GPT-2 and OLMo and absent in classic sentence embedders on the same probe, whose raw $\delta$ equals that of vision-contrastive models yet reflects only their covariance spectrum. Where present, the form is learned: it vanishes under random weights, weakens under non-hierarchical fine-tuning, and emerges across depth. (2)~The \emph{content} is largely shared, but only measured rightly: a naive tree-to-tree comparison (Euclidean dendrograms) shows the label-free family as an island (ARI $\le 0.06$ against every supervised model), which metric and linkage controls reveal to be a chaining artifact; in each family's appropriate metric, every training recipe substantially recovers the human taxonomy (true-superclass ARI $0.48$--$0.60$), with supervision deepening the resemblance. A shared relational base survives permutation calibration, and hyperbolic distances increase cross-model agreement nowhere. (3)~As a corollary, the calibrated descriptors support a modest, training-free decision rule: they predict when leaving Euclidean distances pays on prototype-based tasks, and the training objective selects which zero-cost metric collects the gain. A class-subsampling study shows the operative variable is the depth of hierarchy spanned by the class set, not the number of classes: tree-like form is a property of the (model, concept set) pair, and raw $\delta$ alone cannot tell you where it is. The answer to the title question: largely yes; the shared tree is the human taxonomy, its resemblance graded by supervision, absent only where genuine tree form itself is absent, and visible only under calibrated measurement, on the concept sets tested.
\end{abstract}

\section{Introduction}
\label{sec:introduction}

Semantic knowledge is hierarchical: instances belong to categories (a husky is a dog), categories to superordinate classes (dogs are mammals), and superordinate classes to broader concepts. The natural geometric home for such hierarchies is hyperbolic space \citep{nickel2017poincare, sarkar2011low}, and a long line of work \emph{imposes} it at training time through specialized architectures, losses, or trainable Poincar\'{e} embeddings \citep{Khrulkov_2020_CVPR, chen2022fully, desai2023meru}. A complementary observation, latent hyperbolicity, holds that standard Euclidean models already organize concepts in a tree-like metric structure \citep{Khrulkov_2020_CVPR, yang2025hyperbolic}. Figure~\ref{fig:intro_concept} frames the underlying question: an observer of reality only sees projections of an unknown structure; is that structure hierarchical, and for whom?

The na\"ive measurement of this question is confounded. Gromov $\delta$-hyperbolicity \citep{Gromov1987} assigns $0$ to trees, but three artifacts push raw readings down without any hierarchy: random high-dimensional clouds concentrate toward equidistance, a degenerate star tree; anisotropic covariance spectra mimic low-dimensional behavior; and small class counts shrink the quadruple population the estimator sees. As we show, these artifacts are not hypothetical: a GPT-2-S representation sits \emph{above} its matched null (its low raw $\delta$ is entirely artifact), and classic sentence embedders share their raw $\delta$ values with vision-contrastive models while carrying none of their genuine structure. % exp18_text_nulls.csv
Prior latent-hyperbolicity readings, including our own previous uncalibrated readings, use the confounded scale.

Our first contribution is therefore an \textbf{instrument}: the exact estimator calibrated on reference geometries (trees, hyperbolic regions, spheres, Gaussians of growing dimension), every reading expressed as an \emph{excess} over a null matched in dimension and covariance spectrum with a per-cell $z$-score, a parallelogram estimator of the curvature \emph{sign}, and a class-subsampling control that separates hierarchy depth from class count. With the instrument in hand, we ask the question that recent convergence work \citep{huh2024platonic, groger2026aristotelian} asks of similarity, now of geometry: \emph{what survives calibration, and is it shared across models? Is there a Platonic tree?}

The answer has two halves, which the title condenses. \textbf{Form is widespread, and where it exists it is genuine and learned:} 69 of 72 vision model$\times$dataset combinations sit below their matched null (41/48 on the four hierarchical-label datasets beyond two null standard deviations), the excess deepens with model scale within each transfer dataset, a direct curvature-sign estimator turns increasingly negative with scale for self-supervised vision, and ablations (random weights, non-hierarchical fine-tuning, layer-wise emergence, label shuffles) tie the structure to learning. % exp20_null_ztable.csv, exp12, fig3_causal
In text, the same instrument reveals heterogeneity that raw $\delta$ hides: genuine form emerges with scale in GPT-2 (excess $+0.048 \to -0.048$ from S to XL) and in OLMo (at null at 1B, genuine at 7B), is present at every Pythia scale, and is absent in classic sentence embedders on the class-name probe, although the same embedders show genuine excess on a real text hierarchy, so the form is a property of the (model, concept set) pair. % exp18, exp14
\textbf{The tree is largely shared, once measured rightly:} a naive tree-to-tree comparison shows the label-free family as an island; metric and linkage controls reveal the island as a chaining artifact, and in each family's appropriate metric every training recipe substantially recovers the human taxonomy, with supervision deepening the resemblance and hyperbolic distances increasing cross-model agreement nowhere. As a corollary, the descriptors support a modest, training-free metric-selection rule whose statistics we report with the caution they deserve.

\textbf{Our contributions are:}
\begin{enumerate}[topsep=0pt,itemsep=1pt,leftmargin=*]
\item \textbf{A calibrated instrument for tree-likeness}: estimator calibration, spectrum-matched nulls with per-cell $z$-scores, curvature-sign estimation, and a class-count control, together with the demonstration that raw $\delta$ readings mislead (identical raw values, opposite null verdicts).
\item \textbf{A calibrated census}: in vision, genuine tree form in 69/72 model$\times$dataset cells, objective-ordered, scale-deepening on the excess basis, and learned; in text, form that depends on recipe and scale, including the monotone emergence of genuine structure across the GPT-2 family.
\item \textbf{The map of trees, with its own calibration lesson}: a direct tree-to-tree comparison across models whose naive version manufactures an island (metric--linkage chaining) and whose controlled version shows graded sharing of the human taxonomy across all training recipes, together with calibrated evidence that a relational base is genuinely shared while hyperbolic reading buys no cross-model agreement.
\item \textbf{A scoped, training-free corollary}: when to leave Euclidean distances (predicted by the descriptors; robust statistics reported per claim) and which zero-cost metric collects the gain (set by the training objective), validated across tasks and transferred to a text hierarchy, with its negative results stated.
\end{enumerate}

\begin{figure}[t]
  \centering
   \includegraphics[width=0.78\linewidth, trim=150 220 200 190, clip]{figures/fig_intro_concept.pdf}
  \caption{The title question. An observer (a foundation model, evoking the prisoner in Plato's cave) sees projections of an underlying structure. We ask whether the class geometry each model learns is tree-like (left), embedding with low distortion in the Poincar\'{e} disk, or flat (right); and, across models, whether they share one tree, a Platonic tree, or each grows its own.}
  \label{fig:intro_concept}
\end{figure}

\section{Related Work and Background}
\label{sec:related}

\paragraph{Hyperbolic deep learning: imposing vs measuring.} Building on tree embeddability \citep{sarkar2011low} and Poincar\'{e} embeddings \citep{nickel2017poincare, ganea2018hyperbolic}, hyperbolic methods span vision \citep{Khrulkov_2020_CVPR, ermolov2022hyperbolic, atigh2022hyperbolic, bdeir2024fully}, vision--language \citep{desai2023meru, pal2025compositional}, graphs \citep{chami2019hyperbolic}, and language \citep{chen2022fully, he2026helm, sinha2024learning}. Latent hyperbolicity, low measured $\delta$ in standard models, has been reported for CNN/ViT features \citep{Khrulkov_2020_CVPR, bdeir2024fully}, token embeddings \citep{yang2025hyperbolic}, and word vectors \citep{tifrea2019poincare}. None of these readings is calibrated against matched nulls; our results show that without calibration they are not interpretable, and with it they become a rich comparative signal.

\paragraph{Geometric analyses of representations.} Representation similarity \citep{kornblith2019cka}, intrinsic dimension \citep{ansuini2019intrinsic, pope2021intrinsic}, and neural collapse \citep{papyan2020prevalence} characterize Euclidean structure without testing tree-likeness. Estimating Gromov hyperbolicity on sampled quadruples has known bias and scale caveats \citep{fournier2015computing}, which our calibration-plus-null design addresses operationally.

\paragraph{Representational convergence.} The Platonic Representation Hypothesis \citep{huh2024platonic} argues that models converge toward shared representations; \citet{koepke2026cave} stress-test cross-modal convergence at scale, and \citet{groger2026aristotelian} show that permutation-calibrated similarity removes most apparent global convergence while local neighborhood structure survives. We are the geometric complement of this program: the same calibration philosophy applied within models, and, in Section~\ref{sec:content}, their permutation calibration applied to our cross-model measures, where it locates exactly which part of class geometry is shared (a relational base that survives calibration) and which is not (the hierarchical assembly, which no hyperbolic reading aligns).

\paragraph{Background.} For four points $w,x,y,z$ of a metric space, sort the three pairing sums $d(w,x){+}d(y,z)$, $d(w,y){+}d(x,z)$, $d(w,z){+}d(x,y)$ as $S_{\max}\ge S_{\mathrm{mid}}\ge S_{\min}$; the four-point defect is $(S_{\max}-S_{\mathrm{mid}})/2$ and $\delta$ is its supremum. Trees have $\delta=0$; under this four-point definition the hyperbolic plane has $\delta=\ln 2\approx0.693$ \citep{nica2016strong}, distinct from the thin-triangles constant $\ln(1+\sqrt2)\approx0.881$. We use the normalized $\delta_{\text{norm}}=\delta_{\max}/\mathrm{diam}$ throughout: on this scale hyperbolic regions drift to $0$ as they grow while spheres stay high, so the intuitive ordering trees $<$ hyperbolic $<$ spherical holds. Ollivier--Ricci curvature \citep{ollivier2009ricci} on a kNN graph is our local companion: positive on tightly clustered neighborhoods, negative on tree-like bridges.

\section{The Instrument}
\label{sec:instrument}

This section builds the measurement layer; every later claim is stated in its units. The message: raw $\delta_{\text{norm}}$ is interpretable only against calibrated references and matched nulls.

\paragraph{Class representations.} Each frozen model is a fixed feature extractor; classes are represented by centroids (vision: mean of $100$ training embeddings per class; text: embeddings of ``a photo of a \{class\}'' over the 1000 ImageNet classes, plus the corpus variations of \S\ref{sec:form}). The calibrated census covers 12 vision backbones $\times$ 6 datasets and 16 text models; 4 additional contrastive vision backbones are measured on ImageNet. % TODO-coauthors: confirm final panel counting; panel table in App. A
Estimator settings: $5{\times}10^5$ quadruples per seed, 10 seeds (exact enumeration when $C\le 30$); ORC on $k{=}10$ kNN graphs (ranking invariant for $k\in\{5,\dots,50\}$).

\begin{table}[t]
\centering
\small
\begin{tabular}{lcc}
\toprule
space & absolute $\delta$ & $\delta_{\text{norm}}$ \\
\midrule
balanced binary tree (depth 10) & 0.000 & 0.000 \\
$\mathbb{H}^2$ ($K{=}-1$), region radius $R{=}2/4/8/16$ & 0.65/0.69/0.69/0.69 & 0.162/0.087/0.043/0.022 \\
uniform $S^{99}$ (chord / geodesic) & 0.25/0.37 & 0.143/0.179 \\
iid Gaussian, $n{=}1000$, $d{=}192/768/1536$ & --- & 0.104/0.061/0.046 \\
\bottomrule
\end{tabular}
\caption{Calibration on reference geometries: the estimator recovers the tree zero and the four-point constant of $\mathbb{H}^2$, spheres stay high, and iid Gaussians drift low as $d$ grows, the dimension confound. % exp1_delta_controls.csv, exp6
}
\label{tab:calibration}
\end{table}

\paragraph{Calibration.} Table~\ref{tab:calibration} fixes the landmarks and exposes the confound: random clouds of growing dimension approach the degenerate star tree. A low raw value is not evidence.

\paragraph{Matched nulls and the excess.} For every (model, class set) cell we synthesize null clouds with the same $n$, $d$, mean and covariance \emph{spectrum} (Gaussian coefficients recombined with the real singular values; a PC-permutation null confirms the readings), and report the \textbf{excess} $\delta_{\text{norm}}^{\text{real}}-\delta_{\text{norm}}^{\text{null}}$ with a $z$-score against the combined null-replicate and estimator noise (5 null replicates). ``Tree-like'' below always means \emph{negative excess}, with significance stated; sign-positive excess means the configuration is \emph{less} tree-like than chance given its spectrum. Whitening is not a usable alternative (it collapses centroids toward a regular simplex, itself a degenerate star).

\paragraph{Curvature sign.} $\delta$ diagnoses tree-likeness, not curvature. A parallelogram estimator $\xi$ (midpoint defect of triangles; $0$ Euclidean, positive spherical, negative hyperbolic) is calibrated to separate regimes cleanly: depth-9 tree $-0.24$ and $\mathbb{H}^2$ region $-0.19$ (96\% of triangles negative) versus iid Gaussian $+0.09$ and spheres $+0.11$ to $+0.12$ (0\% negative). We estimate the sign only, never a curvature value. % exp12_curvature_sign.csv

\paragraph{Class-count control.} Because $C$ sets the quadruple population, we sweep ImageNet subsets $C\in\{10,\dots,1000\}$ with two compositions: random (spanning the full hierarchy) and WordNet-coherent (siblings, an effectively \emph{flat} set). Results in \S\ref{sec:form}; the control turns ``needs a real hierarchy'' from a post-hoc label into a measured variable.

\paragraph{Projection and tasks (for the corollary).} The parameter-free map $\phi(x)=\tanh(\tfrac12\|s(x-\mu)\|)\cdot\frac{s(x-\mu)}{\|s(x-\mu)\|}$ centers features, scales the 95th-percentile norm to radius $1/\sqrt2$, and preserves directions; prototypes in the ball are means of projected points re-mapped through the origin. Tasks: nearest-centroid (NC) and 5-way 5-shot (FS, 1000 paired episodes); metric arms: Euclidean (R), Poincar\'{e} (H), the radial map with Euclidean distances (RT), cosine (COS), and the L2-normalized stack. Note the distinction between deforming the configuration (sphericizing raises $\delta_{\text{norm}}$ for all 12 vision models under the geodesic) and changing the readout metric on the intact configuration, which is what the rule selects among. % exp1, exp2

\section{Findings I: Form, Where It Exists, Is Genuine, Learned, and Scale-Deepening}
\label{sec:form}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/fig_excess_panel.pdf}
\caption{(a) Excess per vision cell (12 models $\times$ 6 datasets): 69/72 sign-negative; the exceptions are DINOv2-S/B/G on ImageNet, all within $+1.2$ null s.d. (b) On transfer datasets the excess deepens with scale, while DINOv2's ImageNet excess stays at null. % exp20_null_ztable.csv, exp11
}
\label{fig:excess}
\end{figure}

\paragraph{Vision: 69/72 cells, stated at two levels.} Sign: 69 of 72 cells have negative excess; the three exceptions are DINOv2-S/B/G on ImageNet, their pretraining-like distribution, and none of the three is significantly \emph{above} its null. Significance: 51/72 cells lie beyond two null standard deviations; on the four hierarchical-label datasets (ImageNet, CIFAR-100, CIFAR-10, DTD) 41/48 do, and 34/48 fall strictly beyond three. The two flat datasets are sign-consistent (24/24), but their smaller excesses sit at the resolution limit of the estimator at $C{=}10$, which the class-count control below quantifies. Per-cell values, $z$-scores and Holm-corrected verdicts are in Appendix~\ref{app:tables}. % exp20_null_ztable.csv
On ImageNet, supervised ViTs sit at excess $-0.020$ to $-0.030$ and CLIP/SigLIP at $-0.019$ to $-0.025$; DINOv2-L is sign-negative but marginal there ($z=-1.1$), while within each transfer dataset the same family's excess deepens with scale (S$\to$G on CIFAR-10: $-0.095\to-0.157$; Figure~\ref{fig:excess}b). Because excess magnitudes shrink with $C$ (class-count control below), cross-dataset excess comparisons are not meaningful and all statements here are within-dataset trends. The curvature sign agrees: every model sits below the iid-Gaussian reference, and DINOv2 crosses into negative territory monotonically with scale ($\xi=-0.020\to-0.070$, 66\%$\to$89\% negative triangles S$\to$G), while supervised and contrastive families remain mildly positive yet far below the sphere band. % exp11, exp12

\paragraph{Text: the instrument reveals what raw $\delta$ hides.} Figure~\ref{fig:textnulls} is the census that raw readings cannot deliver. Within causal LMs, genuine form \emph{emerges with scale} in two independent families: monotonically in GPT-2 (excess $+0.048$ at S, $-0.013$ at M, $-0.034$ at L, $-0.048$ at XL) and in OLMo, at null at 1B ($-0.001$) and genuine at 7B ($-0.015$). Pythia carries genuine form at every scale ($-0.035$ to $-0.041$). The rising nulls of this table are themselves explained: they track the effective rank of the centroid covariance (GPT-2: $1.3 \to 56$ from S to XL), and genuineness is not a proxy for anisotropy, since the embedder with the \emph{highest} effective rank (GTE-base, $98$) sits at its null. A PC-permutation null, which preserves each principal component's empirical marginal, confirms every verdict, with one nuance: GPT-2-S's sign-positive excess is specific to the Gaussian spectrum null (PC-permutation $z=-1.4$, n.s.), so the safe statement is that S carries no genuine form under either null while M/L/XL are genuinely negative under both ($z=-3.7/-7.8/-6.4$). % exp18b_text_anisotropy.csv Classic sentence embedders (BGE/GTE/E5, base and large) sit at their nulls on this probe ($+0.001$ to $+0.011$) despite raw $\delta_{\text{norm}}$ values ($0.114$--$0.124$) indistinguishable from vision-contrastive models whose excess is genuine: \emph{identical raw values, opposite verdicts}, the cleanest demonstration that the uncalibrated scale is not interpretable. The only LLM-backboned embedder tested (GTE-Qwen2-1.5B) is genuine ($-0.019$). % exp18_text_nulls.csv
Two scopes: causal-LM geometry is template-sensitive (reported as template-conditioned; range up to $.065$ across eleven templates, while embedders vary by $\le.012$ including a definition-only template with no class names), and the same embedders that sit at null on class-name prompts show genuine excess ($-0.020$ to $-0.028$) on a real three-level text hierarchy (DBpedia), so presence of form is a property of the (model, concept set) pair. % exp4+exp17, exp14

\begin{figure}[t]
\centering
\includegraphics[width=0.9\linewidth]{figures/fig_text_nulls.pdf}
\caption{The calibrated text census (excess with combined null+estimator error bars): genuine form emerges with scale in GPT-2 and OLMo, is constant in Pythia, and is absent in classic sentence embedders on the class-name probe, whose raw $\delta$ equals that of genuinely structured vision models. % exp18_text_nulls.csv
}
\label{fig:textnulls}
\end{figure}

\paragraph{Hierarchy depth, not class count.} At fixed $C$, random ImageNet subsets, which span the full hierarchy, are consistently more tree-like and more exploitable than WordNet-coherent sibling subsets, which form an effectively flat concept set (DINOv2-L at $C{=}50$: excess $-0.124$ vs $-0.093$; NC advantage $+1.58$ vs $+0.53$pp). Raw $\delta_{\text{norm}}$ \emph{rises} with $C$, so the high raw values of the 10-class datasets are not a class-count artifact, and the flat-hierarchy verdict for MNIST stands if anything understated. DINOv2-G reaches its null at $C\ge500$ on ImageNet, independently reproducing the census exception. Full sweep in Appendix~\ref{app:tables}. % exp19_c_sweep.csv

\paragraph{The form is learned.} Four interventions (Figure~\ref{fig:causal}): random weights raise $\delta$ by 65--144\%; fine-tuning on a task with no class hierarchy raises it by 19--91\% while hierarchical fine-tuning leaves it near baseline ($-8$ to $+6\%$); $\delta$ falls across transformer depth (DINOv2-B $-51\%$, CLIP-B $-29\%$); and shuffling embedding-to-label correspondence leaves it unchanged ($p>0.12$), ruling out dependence on point-cloud statistics alone. All are within-architecture comparisons at fixed dimension, immune to the dimension confound. % fig3_causal, original ablations
On matched architecture (ViT-B trained supervised, DINO, or CLIP), all three objectives yield genuine form; the large family separations of the census emerge with scale rather than at fixed size. % night/arch_matched_triplet.csv

\paragraph{ORC: clusters and bridges.} High mean ORC coexists with tree-likeness because they live on different edges: negative curvature concentrates on between-superclass bridges (ImageNet family means: ViTs 7.6\% negative on bridges vs 3.2\% within; CLIP/SigLIP 8.0\% vs 4.4\%), while the mean measures within-cluster compactness. The reported ORC statistics are therefore the bridge quantities; DINOv2 on ImageNet inverts the pattern ($0.3\%$ vs $0.5\%$), consistent with its at-null excess there, and recovers it on CIFAR-100. % night/orc_bridges.csv

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/fig3_causal.pdf}
  \caption{Ablations tying the form to learning. (a)~Random weights: $+65$--$144\%$ higher $\delta$. (b)~Non-hierarchical fine-tuning raises $\delta$ 19--91\%; hierarchical fine-tuning does not. (c)~$\delta$ falls across depth. (d)~Label shuffles leave $\delta$ unchanged.}
  \label{fig:causal}
\end{figure}

\section{Findings II: The Tree Is Largely Shared, Once Measured in the Right Metric}
\label{sec:content}

If the measured trees were a shared representation of reality, models should agree with each other and with human taxonomy. Whether they appear to depends, it turns out, on how the trees are measured, and the difference between the naive and the calibrated answer is the paper's thesis in miniature.

\paragraph{A naive map of trees, and its artifact.} We extract each model's hierarchy explicitly (a dendrogram over the shared class centroids) and compare hierarchies, not representations, with two classic measures: ARI between cut partitions and cophenetic correlation. Under the naive configuration, Euclidean distances with average linkage, the map (Figure~\ref{fig:treemap}) is spectacular: supervised ViTs and contrastive VLMs form one block (cross-family ARI up to $0.82$, WordNet alignment $0.38$--$0.51$), while the label-free DINOv2 family appears as an island (ARI $\le 0.06$ against every supervised model, $0.01$--$0.02$ against WordNet). % exp22_tree_similarity_{imagenet,cifar100}.npz
The island, however, is largely a measurement artifact. Its cuts are degenerate: DINOv2's Euclidean average-linkage dendrograms chain, placing up to $97\%$ of classes in one cluster, which forces ARI $\approx 0$ against any partition by construction; and Euclidean distances are the metric our own diagnostics identify as wrong for this family, whose semantics concentrate in the angular component (\S\ref{sec:form}). % exp23_treemap_controls.npz
Under cosine-Ward, the appropriate metric with a non-chaining linkage, the island largely dissolves: on CIFAR-100, DINOv2-B/L/G agree with the supervised block at ARI $0.56$ (block-internal agreement: $0.61$), cophenetic correlations become indistinguishable ($0.74$ vs $0.74$), and DINOv2 recovers the \emph{true} 20 superclasses at ARI $0.48$--$0.52$ (other families: $0.54$--$0.60$), up from $0.003$--$0.12$ in the naive configuration. % exp23, ground-truth checks
On ImageNet a moderate gap persists in every configuration (e.g.\ cosine-average $0.38$ vs block $0.48$; global WordNet correlation $0.13$--$0.24$ vs $0.46$--$0.57$ under either metric), consistent with DINOv2's tree form being at its null precisely there: where there is no genuine tree, there is no tree to share.

\paragraph{The corrected picture.} Tree content is \emph{largely shared and graded, not categorical}: every training recipe substantially recovers the human taxonomy once its tree is read in its own metric, supervision deepens the resemblance (superclass ARI $0.48$--$0.52$ label-free vs $0.54$--$0.60$ supervised), and apparent islands were manufactured by measuring an angular family with a chaining Euclidean procedure. This is the answer to the title question: the Platonic tree exists, largely, and it is ours, the human taxonomy, its resemblance deepened by supervision and visible only under calibrated measurement; what no reading buys is extra cross-model agreement from hyperbolic distances.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/fig_tree_similarity.pdf}
\caption{The naive map of trees (Euclidean distances, average linkage): pairwise ARI between dendrogram cuts of the 12 vision models, with the human reference as the last row (ImageNet: WordNet; CIFAR-100: the true superclasses). The apparent DINOv2 island is a chaining artifact of this configuration: under cosine-Ward it largely dissolves (\S\ref{sec:content}; Appendix Table on tree-map controls). % exp22, exp23
}
\label{fig:treemap}
\end{figure}

\paragraph{Whose tree?} Spearman correlation between inter-centroid distances and WordNet tree distances on ImageNet: $+0.57$ to $+0.59$ for CLIP/SigLIP, $+0.49$ to $+0.53$ for supervised ViTs, $+0.36$ for DINO-B, and only $+0.18$ to $+0.22$ for DINOv2, the most tree-like family, with shuffle and Gaussian controls at $\approx0$. % exp3_alignment.csv
Cutting centroid dendrograms recovers the 20 true CIFAR-100 superclasses at ARI up to $0.61$ (four linkage methods; permuted labels $\approx0$). % exp8_p1_recovery.csv
Three controls bound circularity: WordNet-coherent groupings are more tree-like than random groupings in 11/12 models; single-image (no pooling) distances still correlate with WordNet ($\rho$ up to $+0.26$); and the decoupling above is the opposite of what label injection would produce. % exp1, exp8_p6_pooling.csv
DINOv2's semantics concentrate in the angular component (sibling triplet agreement $0.91/0.85$ under cosine vs $0.71/0.63$ Euclidean for L/G): strong local-angular semantics, flat global taxonomy. % exp10_local_vs_global.csv

\paragraph{What is shared, calibrated.} Applying the permutation-calibration of \citet{groger2026aristotelian} to our cross-model measures: local neighborhood agreement survives calibration (mutual-kNN $0.474$ raw over the 66 pairs, null $0.010$, calibrated $0.464$), and, unlike in raw representation space, so does global feature alignment on class centroids (linear CKA $0.636$ raw, permutation null $0.107$, calibrated $0.529$): class centroids distill a genuinely shared relational base, the common human world in which every model separates animals from vehicles. % exp21_local_global.csv
The Poincar\'{e} reading improves neither (kNN $0.474 \to 0.427$; CKA $0.636 \to 0.644$): the hierarchical assembly is precisely the part of the structure that is not shared. Together with the map of trees, this sharpens the convergence picture: models share relational content and tree-like form; the tree itself follows each model's supervision, extending to geometry what calibrated similarity analyses report for representations \citep{groger2026aristotelian}.

\section{Corollary: A Modest, Training-Free Metric-Selection Rule}
\label{sec:corollary}

The census implies a practical question: given a frozen backbone and a labeled dataset, which zero-cost metric should prototype-based tasks use? The calibrated descriptors answer the \emph{whether}; the training objective answers the \emph{which}.

\paragraph{Whether to leave Euclidean.} The NC advantage of the Poincar\'{e} projection correlates with $\delta_{\text{norm}}$ at $r=-0.45$ pooled, robust under a cluster bootstrap over datasets (CI95 $[-0.60,-0.36]$), and within datasets at $-0.83$ (ImageNet, CI $[-0.97,-0.65]$) and $-0.87$ (CIFAR-100, CI $[-0.97,-0.78]$); the within-dataset correlations survive partialing out embedding dimension and the Euclidean baseline accuracy (partial $r=-0.62$ ImageNet, $-0.79$ CIFAR-100), so they are not a proxy for backbone quality. % partial correlations computed from table1_regenerated + exp20 For FS the pooled correlation is not robust (CI $[-0.50,+0.12]$; 56/60 cells gain, counted from the unrounded per-cell means, compressing variance) and the claim is within-dataset only: $-0.67$ to $-0.78$ on CIFAR-100/10/DTD, all excluding zero. % night/correlation_cis.csv, table1_regenerated.csv
On flat-hierarchy datasets no metric helps and no correlation survives. Headline effects carry per-episode paired CIs ($\pm0.03$--$0.20$pp) and a McNemar test on the largest NC gain (DINOv2-L CIFAR-100 $+2.05$pp: 242 vs 37 discordant decisions, $p\approx4\times10^{-38}$). % exp13_mcnemar.csv

\paragraph{Which metric collects the gain.} The identical radial map with Euclidean distances changes little (mean $|\mathrm{RT{-}R}|=0.20$pp FS / $0.12$pp NC, no systematic sign), so the H$-$R gain is the metric. Against cosine the split follows the objective: cosine suffices for DINOv2 (whose semantics are angular), H $\approx$ cosine for supervised ViTs, and H beats cosine for contrastive CLIP by $+0.9$ to $+1.3$pp on transfer datasets even after L2 normalization (ImageNet $+0.1$ to $+0.2$pp). % exp2_metric_controls.csv, exp2b
Cosine alone already improves on Euclidean in most hierarchical cells, so on hierarchical data the \emph{whether} question has a high base rate; the diagnostic's discriminative value concentrates on flagging the flat regimes, where nothing helps, and on the \emph{which} question, which the training objective resolves. The advantage of the best zero-cost metric over Euclidean is itself predicted by $\delta_{\text{norm}}$ (FS $r=-0.31$ pooled, $-0.33$ hierarchical; Figure~\ref{fig:bestmetric}), and it is not an optimistic artifact of selecting on test: choosing the metric per cell on a held-out half of the episodes and evaluating on the other half retains the full advantage ($+0.63$pp vs the test-oracle's $+0.64$pp over the 60 cells; $+0.78$pp both on hierarchical cells). With no validation data at all, the objective-based rule modestly beats the always-cosine heuristic ($+0.41$ vs $+0.28$pp on hierarchical cells), so the practical ordering is: validation pick if a split is available, the objective rule otherwise, Euclidean on flat labels. % exp24_val_metric_selection.csv Robustness to the projection target: the FS advantage is positive at every $t$ tested (the a-priori headline $t=1/\sqrt2$ is conservative for FS); NC is sensitive to $t$, its mean advantage crossing zero near the headline target, consistent with NC gains being concentrated in the DINOv2 cells rather than broad. % exp2, night/t_sweep.csv

\paragraph{Transfer and negatives.} Transplanted to DBpedia (three-level text hierarchy, 219 classes, three embedders), the rule predicts correctly on both sides: genuine geometric excess and $0.88$ sibling-triplet alignment, yet $\delta_{\text{norm}}$ above the low band, and indeed no meaningful task gain: FS $+0.04$ to $+0.08$pp at a $\sim$99\% ceiling, and NC \emph{losses} of $-0.4$ to $-0.8$pp. % exp14_dbpedia.csv
Sample-level tasks do not benefit anywhere: kNN loses $2.6$pp on average and sample retrieval improves in 0/60 cells, because the detected hierarchy lives at the prototype level while the radial map distorts local neighborhoods. The domain of validity is prototype-based metric tasks, and the rule is a modest one: typical gains are $+0.5$ to $+2$pp at zero training cost. % exp15, tasks_t707

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/fig_bestmetric_scatter.pdf}
\caption{Advantage of the best zero-cost metric over Euclidean per cell vs $\delta_{\text{norm}}$: more genuinely tree-like cells carry larger exploitable gains (FS $r=-0.31$ pooled, $-0.33$ hierarchical). % figures/make_figs.py, exp2+exp11
}
\label{fig:bestmetric}
\end{figure}

\section{Discussion and Limitations}
\label{sec:discussion}

\paragraph{What the census establishes.} Tree-like form, measured beyond matched nulls, is the widespread but not universal shape of class geometry in foundation models: near-universal in vision, recipe- and scale-dependent in text, always relative to a concept set. Its content is largely shared: measured in each family's appropriate metric, every training recipe substantially recovers the human taxonomy, supervision deepens the resemblance, and the appearance of model-specific trees under naive comparison is itself a measurement artifact, the paper's calibration lesson repeated at the level of tree comparison. Claims about ``the'' geometry of foundation models should be read accordingly, and raw readings, of $\delta$ or of tree similarity, in prior work and in our own previous uncalibrated analyses, are not interpretable without the instrument.

\paragraph{Excess measures existence; raw $\delta_{\text{norm}}$ predicts exploitability.} The two dissociate in both directions (DINOv2 on ImageNet: at null yet positive FS gains; DBpedia embedders: genuine excess yet no meaningful gain). The corollary rule uses the raw descriptor because that is what predicts the task contrast; the excess is the existence proof. Conflating them overstates both.

\paragraph{Limitations.} (i)~We estimate the curvature sign, never a value; descriptive claims concern tree-like metric structure. (ii)~The vision census is class-centroid geometry on six datasets; other concept sets may expose different slices of structure, as the probe-dependence of the embedder verdicts shows. (iii)~Causal-LM geometry is template-conditioned. (iv)~The objective-vs-geometry link is correlational across families; the matched-architecture triplet bounds but does not close it. (v)~Downstream scope is prototype-based metric tasks with modest gains; dense prediction, cross-modal retrieval and end-to-end hyperbolic training are out of scope (training-time methods such as MERU are the tools there). (vi)~Low-dimensional visualizations (Figure~\ref{fig:tree}) are illustrations; all quantitative comparisons are made in the full feature space.

\begin{figure}[h]
  \centering
  \includegraphics[width=\linewidth]{figures/fig_tree_visualization.pdf}
  \caption{Illustrative 2-D projection of the 100 CIFAR-100 class centroids (per-panel PCA fit on the 20 super-class hubs; panels preserve 22--30\% of hub variance and 6--10\% of spoke length, so visual compactness is not comparable across panels and quantitative comparisons are made in full dimension). (a)~DINOv2-S, Euclidean; (b)~DINOv2-S after the projection of \S\ref{sec:instrument}; (c)~DINOv2-G.}
  \label{fig:tree}
\end{figure}

\subsubsection*{Ethics Statement}
This work analyzes publicly released models and standard public datasets, involves no human subjects and no personal data, and proposes an analysis methodology and a metric-selection heuristic. We do not foresee ethical risks beyond those already associated with the underlying models and datasets.

\subsubsection*{Reproducibility Statement}
Every number in the paper is annotated in the source with the result file that produces it; all tables are generated by script from those files, and the full pipeline (feature extraction, diagnostics, nulls, calibration, tasks, statistics, figures) with fixed seeds will be released. Estimator settings are stated in \S\ref{sec:instrument}. Computing $\delta_{\text{norm}}$ on 1000 centroids takes seconds on CPU; the full task grid runs in under 30 minutes on one consumer GPU.

\subsubsection*{The Use of Large Language Models}
LLM-based assistants were used, under continuous author direction and review, to draft and edit text, to write analysis scripts, and to run the experiment pipeline on the authors' hardware. All experimental designs, results, verifications, and final claims were checked and approved by the authors, who take full responsibility for the content of this paper.

\FloatBarrier
\bibliographystyle{iclr2027_conference}
\bibliography{references}

\clearpage
\appendix
\section{Full numerical tables}
\label{app:tables}
Every table is generated from the released per-cell result files by \texttt{gen\_appendix.py} / \texttt{gen\_appendix2.py}; the source file of each table is stated in its caption.

\subsection{Model panel}
\input{appendix_tables/tab_a0_models}
\subsection{Vision census: excess and significance per cell}
\input{appendix_tables/tab_b2_ztable}
\subsection{Text census}
\input{appendix_tables/tab_b1_textnulls}
\subsection{Class-count vs hierarchy depth}
\input{appendix_tables/tab_b3_csweep}
\subsection{Tree-map metric and linkage controls}
\input{appendix_tables/tab_b7_treemap}
\subsection{Task accuracies (regenerated)}
\input{appendix_tables/tab_a2_nc}
\input{appendix_tables/tab_a3_fs}
\subsection{Metric controls}
\input{appendix_tables/tab_a4_cos}
\input{appendix_tables/tab_a5_rt}
\subsection{Statistics}
\input{appendix_tables/tab_a6_mcnemar}
\input{appendix_tables/tab_b5_cis}
\input{appendix_tables/tab_b8_valpick}
\subsection{Projection-target sensitivity}
\input{appendix_tables/tab_b4_tsweep}
\subsection{ORC edge types}
\input{appendix_tables/tab_b6_bridges}
\subsection{Prompt robustness}
\input{appendix_tables/tab_a7_prompts}
\subsection{Text-modality transfer}
\input{appendix_tables/tab_a9_dbpedia}
\subsection{Curvature sign}
\input{appendix_tables/tab_a10_curvature}

\end{document}
